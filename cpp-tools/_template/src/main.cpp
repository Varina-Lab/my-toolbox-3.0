#include <iostream>
#include <vector>
#include <string>

// Sử dụng C++ Standard mới nhất để code gọn và nhanh
int main() {
    // Tối ưu hóa I/O
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    std::cout << "Hello! This is a High-Performance C++ Tool." << std::endl;
    std::cout << "Running as Standalone EXE (Static Linked)." << std::endl;

    // Ví dụ code
    std::vector<std::string> tools = {"Rust", "C++", "Go", "C#"};
    
    // Uncomment dòng dưới nếu muốn dừng màn hình để xem kết quả
    // std::cin.get();
    
    return 0;
}
