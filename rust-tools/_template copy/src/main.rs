fn main() {
    // Đây là nơi bắt đầu của công cụ
    println!("Hello, my-toolbox! This tool runs with maximum performance.");
    
    // Giữ màn hình console không tắt ngay (tiện khi chạy tool dạng CLI)
    // println!("Press Enter to exit...");
    // std::io::stdin().read_line(&mut String::new()).unwrap();
}
